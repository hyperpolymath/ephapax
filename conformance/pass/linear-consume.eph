-- SPDX-License-Identifier: EUPL-1.2
-- Conformance: pass/linear-consume
-- Tests: Proper consumption of linear values

region r {
    let s1 = String.new@r("foo")
    let s2 = String.new@r("bar")
    let result = String.concat(s1, s2)
    drop(result)
}
