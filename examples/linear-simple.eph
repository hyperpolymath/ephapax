// SPDX-License-Identifier: EUPL-1.2
// Minimal demonstration: affine can compile, linear cannot (needs explicit drop)
// This shows the difference between affine and linear type checking

fn main() -> i32 {
    let x = 1 + 2;
    let y = x * 3;
    y
}
