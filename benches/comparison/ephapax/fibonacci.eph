// SPDX-License-Identifier: PMPL-1.0-or-later
// Fibonacci benchmark - recursive implementation

fn fib(n: I32): I32 =
    if n <= 1 then
        n
    else
        fib(n - 1) + fib(n - 2)

fn main(): I32 =
    fib(30)
