// Simple test program for bootstrap verification
// SPDX-License-Identifier: PMPL-1.0-or-later

// Fibonacci function
fn fib(n: I32): I32 =
    if n < 2 then
        n
    else
        fib(n - 1) + fib(n - 2)

// Main function
fn main(): I32 =
    fib(10)
